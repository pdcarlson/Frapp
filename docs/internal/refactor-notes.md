Refactoring onChange handler for checkboxes to dedicated handleRoleChange function inside MemberDetailSheet.

Added comprehensive unit tests for `Async States components` in `apps/web/components/shared/async-states.tsx` including `LoadingState`, `EmptyState`, `ErrorState`, and `OfflineState`.
