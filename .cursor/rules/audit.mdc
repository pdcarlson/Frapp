---
description: Performing code quality reviews, security audits, dependency checks, migration reviews, or CI audits
globs:
  - "supabase/migrations/**"
  - ".github/workflows/**"
  - ".coderabbit.yaml"
  - "docs/internal/**"
  - "spec/**"
alwaysApply: false
---

# Audit & Quality Review

Read `.cursor/skills/audit.md` before performing any audit or quality review.

Key points:
- All tables must have RLS enabled — current design uses default-deny with service_role bypass
- Every write endpoint needs PermissionsGuard with explicit `@RequirePermissions()`
- Migrations must follow `{14-digit-timestamp}_{snake_case}.sql` naming and update rollback docs
- DTOs must use class-validator decorators — global ValidationPipe strips unknown fields
- The spec (`spec/`) is the source of truth — implementation follows spec
