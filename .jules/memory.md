Added edge case test for `createPortalSession` in `BillingService` when a chapter is missing (throwing `NotFoundException`).
The test successfully asserts this case, covering line 114 in `apps/api/src/application/services/billing.service.ts`.
