"use client";

/**
 * Placeholder -- will be rebuilt once the api-sdk types are regenerated
 * from the new OpenAPI spec.
 */
export function useMembers() {
  return {
    data: undefined,
    isLoading: false,
    error: null,
  };
}
