# Events & Attendance

## Event Creation

- Admins with `events:create` permission create events with: name, description, location (free text), start time, end time, point value (configurable, default 10), mandatory flag, and recurrence rule (optional).
- **Recurring events:** Admins can set a recurrence rule (weekly, biweekly, monthly). The system generates individual event instances according to the rule. Each instance can be individually edited or canceled. Recurrence rules can be modified (changes apply to future instances only).
- **Recurrence edit and cancellation scope.** `PATCH /v1/events/:id` accepts an optional `scope` in the body and `DELETE /v1/events/:id` an optional `scope` query parameter, each one of `instance` (the default) or `series`. There is deliberately no "this and past" — the rule above allows individual edits and future-only series changes, and nothing else.
  - **`instance`** is what a caller that sends no `scope` gets, and is the pre-existing behavior for updates. Deletes touch one row *except* when the target is a recurring series head, where the successor promotion below also writes the new head and re-points its siblings — so a client must not assume a single-row invalidation there.
  - **`series`** resolves to the series parent first — so the operation behaves the same whichever occurrence the client happens to hold — and then applies to the parent and to every *generated* occurrence that has not yet started. **A generated occurrence whose `start_time` has passed is never deleted and never patched.** That boundary is evaluated server-side from `start_time`, never from a flag the caller sends, because `event_attendance.event_id` is `on delete cascade`: deleting a past occurrence would destroy the record of a meeting that happened and everyone marked at it.
  - **A `series` edit whose head has already started splits the series rather than rewriting the head.** The parent row is both the series *template* that regeneration reads from and the series' own *first occurrence*, so writing the patch to a started head edits a meeting that already happened. Instead the started head is retired into standalone history — its `recurrence_rule` cleared and **no other field written**, so its `event_attendance` rows keep describing the meeting as it actually was — and the earliest *upcoming* occurrence becomes the new series head, carrying the edit and the recurrence rule. Remaining future occurrences are re-pointed at it (or regenerated from it); occurrences that already happened are detached into standalone events, as everywhere else. Guarding the head instead of splitting it does not work: regeneration reads the parent as the template, so a head that stops accepting writes is rebuilt from stale values — a reschedule is silently dropped, and a later rule change reverts an earlier rename.
    - **The series head's id changes**, exactly as it already does when `instance`-scope deletion of a head promotes its successor. The old id keeps resolving to the meeting that took place, which is the right target for a chat event card, a notification deep link or an exported `.ics` — each was created for that occurrence, not for the series.
    - **Times are applied as a shift, not as instants.** The caller's `start_time` describes the old head and lies in the past; moving the head 18:00 → 19:00 moves every upcoming occurrence an hour later rather than anchoring the series on the head's past date. A shift that would push the next occurrence before now is a 400.
    - A `series` edit on a started head with **no upcoming occurrence** is a 400: every row it could reach is history. Edit those with `instance` scope.
    - Clearing `recurrence_rule` in the same edit ends the series; the promoted occurrence survives as a standalone event carrying the edit, rather than the edit landing nowhere.
  - A `series` edit addressed to a **generated occurrence** applies every field except `start_time` / `end_time`. Those describe the occurrence the client had open, not the series anchor — clients round-trip the whole event object, so honouring them turned a rename into a request to move the whole series. Move the anchor by addressing the head. An edit from a generated occurrence carrying *only* times is a 400 rather than a silent no-op.
  - On a `series` update, a change to `recurrence_rule`, `start_time` or `end_time` **regenerates** the future occurrences from the new rule; any other field is patched onto them in place. The comparison is by value, not by presence — a client that PATCHes the whole event back with an unchanged rule does not trigger a regeneration. Regeneration always yields a full series of *upcoming* dates: occurrences falling in the past are skipped without consuming the count, so a long-dormant series regenerates forward rather than to nothing. `recurrence_rule` accepts only `WEEKLY`, `BIWEEKLY` or `MONTHLY`; anything else is a 400 **before** any occurrence is deleted (`null` still clears the series).
  - **Deleting a series parent with `instance` scope promotes the next still-upcoming occurrence** into the series head and re-points its remaining future siblings at it; occurrences that already happened are detached into standalone events rather than promoted, and if none is still upcoming the series simply ends. `events.parent_event_id` is `on delete set null`, so without that promotion cancelling the first occurrence would silently strand the rest of the series as unowned rows that no later series operation could reach.
  - `series` deletion of a series whose parent has already started ends the series in place (clearing its `recurrence_rule`) rather than deleting the parent row, for the same attendance reason; surviving past occurrences are detached into standalone events.
  - Generated occurrences inherit the parent's `check_in_zone` and `check_in_zone_name` along with its other fields, so a recurring meeting's geofence applies to every date rather than only the first.
- **Role-based required attendance:** Events can specify which roles are required to attend via a `required_role_ids` field (text array, nullable). If set, only members with those roles are counted for attendance purposes. If null, the event is either mandatory for all members (`is_mandatory = true`) or optional. This enables targeted meetings (e.g. an exec meeting requires only officers; a scholarship committee meeting requires only members with a scholarship-related role).
  - **Role-targeting also gates read access, not just attendance.** A member whose `role_ids` don't intersect `required_role_ids` gets nothing back from `GET /v1/events` (filtered out of the list), a `404` from `GET /v1/events/:id` and `GET /v1/events/:id/ics` (never a `403`, so the response can't be used to confirm a role-targeted event exists), and no hit for it from `GET /search`. The "New Event"/"Event Updated" push and in-app notifications are scoped the same way — only eligible members are notified, not the whole chapter. A member who holds `events:update` (or the wildcard `*` role) is exempt from all of the above and always sees every event, so an officer who can edit any event is never blocked from finding it to do so.
  - **A role-targeted event cannot be created with a chat card.** `POST /v1/events` returns `400` when `required_role_ids` is non-empty and either `channel_id` or `client_message_id` is supplied (either key alone, not just the pair), and no event row is written. The `kind:"event"` card embeds the event's name, times, location and point value in a chat message that Realtime fans out to every reader of the channel, so posting one would show an ineligible member exactly what `GET /v1/events/:id` returns `404` to hide. It is refused rather than redacted because the card is a snapshot the client renders from its own payload — it never re-reads the event — so a single broadcast row cannot show different viewers different things, and channels gate on `required_permissions` (permission strings) rather than roles, so there is no channel the card could be safely confined to instead. The `/event` slash command is unaffected: it has no syntax for `required_role_ids` and always creates untargeted events. The activity feed is filtered by the same rule — `GET /v1/activity-feed` emits an event's name and location, so it resolves events through the viewer-scoped read, not the chapter-wide one. **Known gap:** `PATCH /v1/events/:id` can still add `required_role_ids` to an event whose card already posted, and that card is left in place — the details were chapter-visible at the moment they were broadcast, and no scrub is attempted (deleting the message would not remove them either, since a deleted row keeps its `payload` — see #1575).
- **`required_role_ids` wire semantics:** omitting the field on create stores `null` (untargeted). On update, omitting the field leaves targeting unchanged; sending an empty array clears it. A stored `[]` is equivalent to `null` everywhere targeting is evaluated — check-in eligibility and the auto-absent sweep both treat a zero-length array as "all members". The values are **RBAC role ids** — rows of the live `roles` table, the same ids carried in `member.role_ids` and served by `GET /v1/roles` — not `chapter_custom_roles` keys (see [`rbac.md`](rbac.md)). The event editor's required-roles multi-select and the event detail's role-name resolution both read that endpoint; an untargeted event's detail view shows "All members".
- **Meeting minutes:** Events have an optional `notes` field (rich text / markdown). Editable by admins with `events:update` permission after the event. Visible to all members who have access to the event (based on required roles or chapter-wide for non-role-targeted events).

## Check-In

- Members check in via the mobile app (self-service).
- **Rotating check-in code (optional assurance).** An officer with `events:update` mints a rotating code from `GET /v1/events/:eventId/attendance/check-in-token` and displays it (mobile s22); a member scans it (s18) and the token rides on the check-in request. The code is HMAC-signed over the event id and a 30-second window, and the server accepts the **current and the immediately previous** window so a scan racing a rotation still lands. A short manual code (`4KQ-88`) is minted from the same secret for members whose camera fails. Signing key: `EVENT_CHECK_IN_TOKEN_SECRET` — where it is unset the mint route returns 503 and a supplied token is rejected, while plain self check-in is unaffected.
  - **"Single-use" means once per member, not once per code.** One host code is scanned by everyone in the room inside the same window; a globally single-use token would admit exactly one member. The one-per-member guarantee is the unique `(event_id, user_id)` index enforced inside `check_in_event`, not a redemption ledger over tokens.
  - **A token is verified when supplied, never required.** The chat event card checks in with no token at all and continues to work. The rotating code raises the effort of sharing a code around; it is not an access control. The geofence below is.
- **Check-in geofence (the anti-proxy control).** An event may carry `check_in_zone` — a polygon of `{lat,lng}` vertices — plus a display name in `check_in_zone_name`. When set, check-in **requires** the member's coordinates on every surface and rejects a position outside the polygon (403); a request that omits coordinates is a 400, and a malformed polygon fails closed rather than being treated as "no zone". When `check_in_zone` is null the event has no geofence and no location is requested. Any displayed code can be screenshotted and forwarded, so presence — not the code — is what the server actually verifies (`spec/ui/mobile/patterns.md` § QR check-in). A surface that cannot supply a location, such as the web chat event card, is told to use the mobile app rather than shown a control that fails.
  - Zones are set through `POST /v1/events` / `PATCH /v1/events/:id` (`events:create` / `events:update`). Sending an empty array **clears** the zone, matching the `required_role_ids` wire semantics above; fewer than three points is a 400.
    - **The two verbs are deliberately asymmetric, and the asymmetry is load-bearing.** `CreateEventDto.check_in_zone` carries `@ArrayMinSize(3)`, so an empty array on create is a 400 rather than "no zone" — a zone-less create must omit the field entirely. `UpdateEventDto` drops that decorator precisely so `[]` can clear a stored zone. A client that sends `[]` on create to mean "no geofence" gets a validation error, not an un-geofenced event.
  - The **dashboard event editor** surfaces the zone as a list of coordinate rows (add / remove / reorder) plus the zone name, and rejects fewer than three points in the form before the API does. Until this shipped the fields were reachable only by hand-crafting an API request, so the geofence was enforced everywhere but creatable nowhere. Drawing a zone on a map is a later addition — it needs a mapping dependency the repo does not carry, and the coordinate rows are the interim surface, not a permanent one.
  - The **event detail view** reports zone state in three ways, not two: a named zone with its point count, no zone at all, or a warning that the stored polygon is malformed. The third case matters because a non-empty polygon with fewer than three valid points fails `isValidZone` and so rejects every check-in — showing it as "no zone" would tell an officer the opposite of what members experience.
- Check-in atomically creates an attendance record AND awards the event's point value (same database transaction), via the `check_in_event` RPC.
- Check-in is only available during the event's time window (between start_time and end_time, with a configurable grace period after end_time, default 15 minutes).
- Unique constraint: one attendance record per (event, user) — enforced inside `check_in_event` via `on conflict do nothing`, so a concurrent double check-in inserts nothing and returns 409 Conflict without a double award. See [points.md](points.md) for the atomicity invariant.
- For role-targeted events, only members with matching roles can check in. Members without the required role who attempt to check in receive a 403 Forbidden.
- Alumni-role members cannot check in and receive a 403 Forbidden, checked before the attendance + points write — **unless** the event names roles in `required_role_ids` and the alumnus matches, which is an explicit decision to include them (e.g. an alumni homecoming). See [alumni.md](alumni.md).

## Attendance Management

- Admins with `events:update` permission can view full attendance for any event.
- **Excuse workflow (admin-only):** Admins mark members as EXCUSED with an optional reason string. Members cannot self-submit excuses. Excused members are not penalized for mandatory events and do not appear as ABSENT in reports.
- Admins can also manually mark members as ABSENT or LATE after the event.
- Marking a member ABSENT who previously checked in (PRESENT) does NOT reverse the points already awarded. The admin must separately create a point adjustment if needed.
- **Auto-absent:** For mandatory or role-targeted events, members who are required to attend but did not check in and were not marked EXCUSED are auto-marked ABSENT after the grace period ends. Alumni-role members are excluded on non-targeted events — they can neither check in nor self-excuse, so marking them would guarantee an ABSENT record they have no way to avoid. A role-targeted event keeps whoever it names, alumni included. This runs automatically: an hourly scheduled sweep processes every event whose grace period closed in the preceding 24 hours, so no officer action is required. The admin-triggered endpoint remains available for immediate marking. Re-running is harmless — members who already hold an attendance record are skipped.

## Edge Cases

- If a role referenced in `required_role_ids` is deleted, that role is effectively ignored for attendance purposes (members who previously held it are no longer required).
- If an event's point value is changed after some members have already checked in, only future check-ins use the new value. Already-awarded points are not retroactively adjusted.

## Calendar Integration

- Events display an "Add to Calendar" action.
- Tapping it generates an `.ics` file (or deep-links to the device's calendar app on mobile) with event name, description, location, start/end time, and a link back to the event in Frapp.
- Recurring events generate recurring calendar entries.

## Chat Integration

The `/event "<name>" <YYYY-MM-DD> <HH:MM>-<HH:MM> [location] [points]` slash command creates an event
(`events:create`, re-checked on `POST /v1/events`) and posts a server-originated **event card** to the
current channel — an immutable snapshot (name, when, location, point value). Every member sees a
**Check-in** action during the event window (start → end + grace), so the chat card is an *additional*
self-service check-in surface alongside the mobile app; the server enforces the window, the role gate, and
one-check-in-per-member regardless of surface. Viewers who can read attendance (`events:update`) also see a
live checked-in count read back through the attendance query (members can self-check-in but can't read the
roster, so the count is shown only to them). Recurrence, role-targeting, and the mandatory flag stay on the dashboard.

Role-targeting and the card are mutually exclusive on create, and `POST /v1/events` enforces that with a `400` —
because the card is broadcast to every reader of the channel, it would undo the read gate. The rule, its reasoning
and its one known gap live with the rest of role-targeting, under
[Role-based required attendance](#event-creation); this section does not restate them.

Pre-event **RSVP intent** (going / maybe / not-going, ahead of the window) is not yet modelled — tracked as
a follow-up. The shared slash-command catalog, dispatch path, renderer registry, and the
server-originated-kind (anti-forgery) rules are canonical in [`integrations.md`](integrations.md) and the
chat-specific [`chat/integrations.md`](chat/integrations.md).
