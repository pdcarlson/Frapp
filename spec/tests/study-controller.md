# StudyController Tests
Unit testing for the `StudySessionController` and `StudyGeofenceController` is included under `apps/api/src/interface/controllers/study.controller.spec.ts`. This confirms payload handling for study sessions and geofences works correctly.
