name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  # Cancellable on a PR ref, NEVER on main. `github.ref` is `refs/heads/main`
  # for every push to main, so an unguarded `cancel-in-progress: true` puts
  # every merge's push run in one group and the first commit's run is cancelled
  # by the second's — leaving required checks concluded `cancelled`, which
  # `validate-deploy-sha.mjs` refuses to deploy. Cancelling a PR run costs a
  # re-push; cancelling a main push run costs a commit's deployability.
  # Full account: scripts/ci/validate-deploy-sha.mjs § CANCELLED_CONCLUSIONS.
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

# ---------------------------------------------------------------------------
# Domain-specific parallel jobs. `packages-build` runs first (shared
# dependency). web-tests, web-responsive-floor, and pglite-migrations are
# path-gated on PRs via the `changes` job and run unconditionally on push;
# everything else always runs.
#
# Path-gated does NOT mean advisory. web-tests and web-responsive-floor are both
# REQUIRED checks — a job skipped by a job-level `if:` reports Success, which
# satisfies one. That is also why `changes` is itself required: a required job
# skipped because a `needs:` parent FAILED also reports skipped, so a
# non-required parent would be a hole. See scripts/ci/lib/required-checks.mjs.
#
# `clean-checkout-typecheck` is the deliberate exception: it depends on nothing
# and prebuilds nothing, so it alone exercises the bare `npm install` path.
# ---------------------------------------------------------------------------

jobs:
  # Change detection: path-gates the web/pglite jobs (Lever D). Itself required —
  # see the header above and scripts/ci/lib/required-checks.mjs.
  # The filter runs only on pull_request; on push the gated jobs run
  # unconditionally (full validation on protected branches), so a shallow
  # checkout here can never fail the gate.
  changes:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      web: ${{ steps.filter.outputs.web }}
      pglite: ${{ steps.filter.outputs.pglite }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Detect changed paths
        id: filter
        if: github.event_name == 'pull_request'
        uses: dorny/paths-filter@v3
        with:
          filters: |
            web:
              - 'apps/web/**'
              - 'packages/**'
              - 'package-lock.json'
              - 'turbo.json'
              # Both gated jobs build `packages/*` through this action, so it is
              # as build-affecting as `turbo.json`. Omitting it would let a PR
              # that edits only the action skip two REQUIRED checks, which
              # report Success when skipped (see the header above).
              - '.github/actions/**'
            pglite:
              - 'supabase/migrations/**'
              - 'scripts/check-pglite-migrations.mjs'
              - 'packages/validation/**'
              - 'package-lock.json'
              # The PGlite job also loads the chapter directory seed, so a change
              # to the seed or its loader must re-run it.
              - 'supabase/seed/**'
              - 'scripts/load-chapter-directory.mjs'
              - 'scripts/lib/chapter-directory-seed.mjs'

  # ── Shared packages build (dependency for downstream jobs) ──────────────
  packages-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      # The one job that writes the cache; every other job restores it.
      - name: Build shared packages
        uses: ./.github/actions/turbo-packages-build
        with:
          save: "true"

  # ── Lint + type check (full monorepo) ───────────────────────────────────
  lint-and-typecheck:
    needs: packages-build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build shared packages
        uses: ./.github/actions/turbo-packages-build

      - name: Type check
        run: npm run check-types

      - name: API production build (nest build / Render parity)
        run: npm run build -w apps/api

      - name: Lint
        run: npm run lint

      - name: Landing unit tests
        run: npm run test -w apps/landing

      # `@repo/validation` is consumed by the API, web, and mobile, so a
      # regression in it ships to all three. It had no suite until #687 added
      # one, and an unrun suite protects nothing.
      - name: Shared validation package tests
        run: npm run test -w @repo/validation

      # Same reasoning as above. The contrast math here is imported by both
      # `packages/theme` and `packages/chapter-theme`; each now has a suite of
      # its own (below), but those pin their callers' behaviour, not this
      # package's, so it still needs its own step.
      - name: Shared color package tests
        run: npm run test -w @repo/color

      # Locale date/datetime helpers plus the three protected clusters
      # (stopwatch padding, bare-date timezone parsing, minute rounding)
      # that must not fold into the generic formatter.
      - name: Shared formatting package tests
        run: npm run test -w @repo/formatting

      # The accent engine. Its contrast guarantee is only as good as the exact
      # behaviour of the vendored generator, so a dependency bump is precisely
      # what this suite exists to catch.
      - name: Chapter theme package tests
        run: npm run test -w @repo/chapter-theme

      # `@repo/theme` owns every token value in the app — the legacy bone/bronze
      # set, the Signet dark set, and the shared Tailwind preset — and until
      # #1144 its only gate was `tsc`, which proves the fields are `string` and
      # `number` and nothing about what they contain. The suite compares them
      # against `spec/ui/design-system/foundations.md`, so it fails whichever
      # side drifts. `lint` needs no step here: it arrives through `turbo run
      # lint` in the `Lint` step above as soon as the package declares a script.
      - name: Theme package tests
        run: npm run test -w @repo/theme

      # Hand-written `statusOf` / `serverMessageOf` / `codeOf` live in
      # `@repo/api-sdk` (consumed by API + web + mobile). Same trap as
      # validation: an unrun suite protects nothing. The package uses the
      # hoisted workspace vitest; this step does not add a lockfile entry.
      - name: Shared api-sdk package tests
        run: npm run test -w @repo/api-sdk

      - name: Brand assets (icon sync)
        run: npm run check:brand-assets

      # ── Clean-checkout guard (fresh-install path) ───────────────────────────
  # Reproduces a bare `npm install && npm run check-types` on a cold machine.
  # Deliberately has NO `needs: packages-build` and NO
  # `./.github/actions/turbo-packages-build` step (which is what restores the
  # turbo cache and builds `packages/*`) — the eight jobs that DO use it are
  # structurally blind to this failure mode, which is why this one must not.
  #
  # The shared packages publish their types as `dist/index.d.ts`, and `dist/` is
  # gitignored, so a consumer resolving the `types` condition (`apps/api`, via
  # NodeNext) sees nothing until those packages are built. That is why `turbo.json`
  # makes `check-types` and `lint` depend on `^build` rather than on themselves.
  # Revert either one and this job goes red while the rest of CI stays green.
  #
  # Keep this job free of build/cache steps — adding one silently disarms it.
  clean-checkout-typecheck:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # `cache: npm` above only restores the ~/.npm download cache, never
      # node_modules or packages/*/dist — the clean-checkout property holds.
      - name: Install dependencies
        run: npm ci

      - name: Type check with no prebuilt packages
        run: npm run check-types

      - name: Lint with no prebuilt packages
        run: npm run lint

  # ── Production-shaped frontend build (Vercel parity) ────────────────────
  # Builds apps/web and apps/landing the way Vercel's PRODUCTION deploy does:
  # a dependency tree with devDependencies pruned. Nothing else in CI does
  # this, and the gap has taken production down twice -- #1331 (@repo/hooks
  # compiled its own test files) and #1372 (both apps' tsconfig pulled
  # tests/, vitest.config.ts and playwright.config.ts into the `next build`
  # type-check program). Both surfaced first as a failed production deploy.
  #
  # Why the existing jobs cannot see this class:
  #   - `web-tests` / `lint-and-typecheck` run on a full dev install, where
  #     vitest and friends are on disk.
  #   - `clean-checkout-typecheck` is a cold install but a *dev* one; it
  #     guards unbuilt package types, not pruned devDependencies.
  #   - A green Vercel preview is not evidence either: previews restore a
  #     build cache (`up to date, audited 1958 packages`) while production
  #     installs cold (`added 1126 packages, and audited 1144`).
  #
  # Deliberately has NO `needs:`, NO `./.github/actions/turbo-packages-build`
  # step (the turbo cache restore + `packages/*` build) and NO path filter:
  #   - `needs: packages-build` would put prebuilt package `dist/` on disk
  #     from a dev tree and hide a package that cannot build under the prune.
  #   - the `changes.web` filter covers `apps/web/**` and `packages/**` but
  #     NOT `apps/landing/**`, so gating on it would have skipped this job on
  #     the landing half of #1372 -- the exact half #1352 failed to record.
  # Keep this job free of cache and prebuilt-package steps; adding one
  # silently disarms it.
  web-production-build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # `npm ci` rather than Vercel's `npm install --prefix=../..`: it is
      # lockfile-exact and never writes package-lock.json, and it reproduces
      # the same tree -- measured at 1128 packages / 1146 audited against the
      # production build log's 1126 / 1144.
      - name: Install production dependencies only
        run: npm ci --omit=dev

      # The prune is this job's entire premise, so assert it rather than assume
      # it. Two probes, one per direction: something dev-only must be gone, and
      # the type-checker must survive.
      #
      # Note that neither is decided by the dependencies/devDependencies split
      # alone -- npm keeps a package the prune would otherwise drop when it is
      # reachable as an optional peer, which the lockfile marks `devOptional`.
      # `typescript` is in that position today, and NOT via Next, which declares
      # no typescript peer at all: the non-dev packages carrying an optional
      # typescript peer are `frimousse` (a direct apps/web dependency) and
      # `@expo/require-utils`. That is a thin thread -- dropping a UI dependency
      # could strip the type-checker from Vercel's production install too -- so
      # treat a failure here as "the tree changed shape",
      # investigate which mechanism moved, and fix that -- do not delete the
      # check to get green, and do not assume the answer is a misfiled
      # dependency.
      - name: Assert the tree is actually pruned
        run: |
          set -euo pipefail
          if [ -d node_modules/vitest ]; then
            echo "::error::node_modules/vitest survived --omit=dev, so this is not a production-shaped tree and this job is silently covering nothing. Check whether vitest became a production dependency or gained an optional-peer path (lockfile 'devOptional')."
            exit 1
          fi
          if [ ! -d node_modules/typescript ]; then
            echo "::error::typescript did not survive --omit=dev. Vercel's production build would lose its type-checker too, so this is a real parity finding, not a CI-only problem: check what stopped keeping it reachable (today it is an optional peer of frimousse and @expo/require-utils -- NOT of next, which declares no typescript peer)."
            exit 1
          fi
          echo "::notice::Production-shaped tree confirmed (vitest pruned, typescript present)"

      # turbo is a root devDependency and is therefore pruned too, so it is
      # fetched the way Vercel's build step gets it. The version is read rather
      # than pinned here a second time -- one source of truth, so a bump cannot
      # leave this workflow building with a different turbo than the repo uses.
      #
      # Read from package-lock.json, not package.json: the lock records what
      # `npm ci` actually installs everywhere else, while the manifest records a
      # RANGE. A lockfile-maintenance bump inside `^2.10.11` would otherwise
      # leave every other job on the new turbo and this one on 2.10.11 -- the
      # exact drift this is meant to prevent. It also does not care whether
      # turbo sits in `dependencies` or `devDependencies`, so moving it (a
      # plausible edit, since this job prunes devDependencies) cannot break the
      # required check for every open PR.
      - name: Build web and landing as production
        # `next build` prerenders, and `/chat` constructs a Supabase browser
        # client while doing so, so the build needs NEXT_PUBLIC_* present or it
        # dies with "@supabase/ssr: Your project's URL and API key are required".
        # These are the stand-ins from `apps/web/playwright.config.ts` — a valid
        # HTTP URL and a well-formed JWT-ish string, enough for the client to be
        # constructible — kept identical so the repo has one placeholder shape.
        #
        # Deliberately NOT the real values, even though Vercel's production
        # build has them via the Infisical sync: `NEXT_PUBLIC_*` is inlined at
        # build time, so real values would bake a live URL and key into a CI
        # artifact that is then thrown away. This job exists to check the
        # dependency shape of the build, not its env — the env only has to be
        # present enough to let the program run. Do not "fix" this by wiring in
        # secrets.
        env:
          NEXT_PUBLIC_SUPABASE_URL: "http://127.0.0.1:54321"
          NEXT_PUBLIC_SUPABASE_ANON_KEY: "eyJhbGciOiJIUzI1NiJ9.playwright-stand-in.signature"
          NEXT_PUBLIC_API_URL: "http://127.0.0.1:3001"
        run: |
          set -euo pipefail
          TURBO_VERSION="$(node -p "require('./package-lock.json').packages['node_modules/turbo'].version")"
          echo "Using turbo@${TURBO_VERSION}"
          npx --yes "turbo@${TURBO_VERSION}" run build --filter=web --filter=landing

      # `turbo run --filter` exits 0 when a filtered package simply has no
      # `build` script -- only an unknown PACKAGE name is an error. So renaming
      # or rerouting either app's `build` script would leave this required check
      # green having built one app, silently uncovering the landing half of the
      # #1372 class that the job exists to cover. Assert the output instead of
      # trusting the exit code.
      - name: Assert both apps actually built
        run: |
          set -euo pipefail
          missing=0
          for app in web landing; do
            if [ ! -f "apps/$app/.next/BUILD_ID" ]; then
              echo "::error::apps/$app/.next/BUILD_ID is absent, so $app was not built even though turbo exited 0. Check that apps/$app/package.json still defines a 'build' script and that the turbo filter still matches its package name."
              missing=1
            fi
          done
          [ "$missing" -eq 0 ]
          echo "::notice::web and landing both produced a production build"

  # ── API unit + E2E tests ────────────────────────────────────────────────
  api-tests:
    needs: packages-build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build shared packages
        uses: ./.github/actions/turbo-packages-build

      - name: Run API tests
        run: npm run test -w apps/api

      - name: Run API E2E tests
        run: npm run test:e2e -w apps/api

      # Adversarial AI evals (spec §13 → Evals). Required to run on any change
      # to prompts, retrieval or the tool registry; run unconditionally instead,
      # which is a superset and costs ~1.5s. A separate path-gated job would add
      # a checkout + `npm ci` + package build — minutes, against ADR-15's budget,
      # to gate seconds of tests.
      - name: Run AI evals
        run: npm run test:ai-evals -w apps/api

  # ── Web unit tests (Vitest / jsdom) ─────────────────────────────────────
  web-tests:
    needs: [packages-build, changes]
    if: ${{ github.event_name == 'push' || needs.changes.outputs.web == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build shared packages
        uses: ./.github/actions/turbo-packages-build

      - name: Run web unit tests
        run: npm run test -w apps/web

      # The shared packages consumed by apps/web that nothing else covers. They
      # live here rather than in their own job because the `changes` filter
      # above already covers `packages/**`, so they run on the right paths
      # without adding a branch-protection entry.
      #
      # `if: !cancelled()` because these are independent suites, not follow-on
      # phases: without it a red apps/web test skips them and hides more
      # failures until the next push. Not `always()` — this workflow sets
      # `cancel-in-progress`, and `always()` keeps running after a cancellation,
      # so every superseded run would still pay for those suites.
      #
      # This job IS a required check as of the ADR-15 amendment — it is the only
      # suite covering packages/hooks. The path gate above is compatible with
      # that: GitHub reports a job skipped by a job-level conditional as
      # "Success". See scripts/ci/lib/required-checks.mjs for the full
      # reasoning and the caveat about skipped-because-a-parent-failed.
      - name: Run shared hooks package tests
        if: ${{ !cancelled() }}
        run: npm run test -w packages/hooks

      - name: Run chat-core package tests
        if: ${{ !cancelled() }}
        run: npm run test -w packages/chat-core

      - name: Run chat-integrations package tests
        if: ${{ !cancelled() }}
        run: npm run test -w packages/chat-integrations

  # ── API Docker image (same path as Render native Dockerfile builds) ─────
  api-docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build API image (Dockerfile)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/api/Dockerfile
          push: false
          # `load: true` (was false) so the next step has an image to run.
          load: true
          tags: frapp-api:ci
          cache-from: type=gha,scope=api
          cache-to: type=gha,mode=max,scope=api

      # A successful build is NOT evidence the image can start, and the gap
      # between the two is not hypothetical: in #1160 `colorjs.io` and later
      # `zod` stopped hoisting to the root node_modules, the runner stage never
      # copied the workspace trees holding them, and `verify-render-api` failed
      # on 67 consecutive pushes to main -- while this job stayed green on every
      # one of them. Render reports that as `update_failed`, a post-build state;
      # the runtime logs show MODULE_NOT_FOUND on first require().
      # Booting the container is the only gate that sees this class of failure.
      - name: Boot the image and probe /health
        run: |
          set -euo pipefail

          # /health answers 200 whether or not the database is reachable -- it
          # reports `database: error` / `status: degraded` rather than throwing
          # (apps/api/src/interface/controllers/health.controller.ts). So these
          # obviously-fake values satisfy the boot-time env validation in
          # apps/api/src/config/env.validation.ts without needing any secret,
          # and a 200 proves the process booted and Nest is serving.
          docker run -d --name frapp-api-ci -p 3001:3001 \
            -e SUPABASE_URL=http://127.0.0.1:1 \
            -e SUPABASE_SERVICE_ROLE_KEY=ci-not-a-real-key \
            -e SUPABASE_ANON_KEY=ci-not-a-real-key \
            -e STRIPE_SECRET_KEY=sk_test_ci_not_real \
            -e STRIPE_WEBHOOK_SECRET=whsec_ci_not_real \
            -e STRIPE_PRICE_ID=price_ci_not_real \
            frapp-api:ci

          # --max-time must clear the unreachable-database path, not the happy
          # one. /health still answers 200 with no database, but only after the
          # Supabase client gives up: measured at ~7s locally against a dead
          # SUPABASE_URL. A short per-request timeout therefore fails on every
          # attempt and turns this gate into a permanent red on all PRs.
          # Bounded by a total deadline instead, so one slow request cannot
          # multiply into a ten-minute hang.
          deadline=$(( SECONDS + 180 ))
          while [ "$SECONDS" -lt "$deadline" ]; do
            # No --show-error: a connection-refused line per attempt would bury
            # the real failure. `docker logs` below is the diagnostic that matters.
            if curl --fail --silent --max-time 25 http://127.0.0.1:3001/health; then
              echo
              echo "::notice::API image booted and answered /health"
              exit 0
            fi
            if [ -z "$(docker ps -q -f name=frapp-api-ci)" ]; then
              echo "::error::Container exited before answering /health -- the image cannot start"
              docker logs frapp-api-ci
              exit 1
            fi
            sleep 2
          done

          echo "::error::/health did not answer within 180s"
          docker logs frapp-api-ci
          exit 1

      - name: Remove the probe container
        if: always()
        run: docker rm -f frapp-api-ci >/dev/null 2>&1 || true

  # ── API contract freshness check ────────────────────────────────────────
  api-contract-check:
    needs: packages-build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build shared packages
        uses: ./.github/actions/turbo-packages-build

      - name: Check API contract freshness (PR)
        if: github.event_name == 'pull_request'
        run: npm run check:api-contract -- --base "${{ github.event.pull_request.base.sha }}" --head "${{ github.sha }}"

      - name: Check API contract freshness (push)
        if: github.event_name == 'push'
        run: npm run check:api-contract

      # Breaking-change detection lives HERE, in the existing contract job,
      # rather than in a job of its own — because a separate "SDK-drift" gate
      # would have been entirely redundant. `check:api-contract` above already
      # regenerates BOTH openapi.json and packages/api-sdk/src/types.ts and
      # fails on any diff, and it is already a required check. What was missing
      # is not drift detection but compatibility: deleting an endpoint or
      # removing a response field regenerates perfectly cleanly and breaks every
      # client.
      #
      # ADVISORY (no --fail-on-breaking): every consumer (apps/web, apps/mobile)
      # regenerates from this repo and ships with the change, and the project is
      # mid-rebuild where removing endpoints is the intended work. It annotates
      # the run rather than blocking it. Revisit if an independently deployed
      # consumer ever appears.
      #
      # `continue-on-error` on BOTH steps, and it is load-bearing: this job is a
      # REQUIRED check, and these steps fetch a binary from the GitHub releases
      # CDN at run time. Without it, a rate limit or a 5xx on that download turns
      # an advisory signal into a merge block on every PR — a required gate whose
      # availability depends on a third-party download.
      - name: Install oasdiff
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: bash scripts/install-oasdiff.sh

      - name: Detect breaking API changes (advisory)
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: npm run check:api-breaking -- --base "${{ github.event.pull_request.base.sha }}"

  # ── Architectural boundary linting (dependency-cruiser) ─────────────────
  # HARD GATE from day one, made survivable by a committed baseline:
  # `.dependency-cruiser-known-violations.json` grandfathers the 7 violations
  # that existed when this landed, and any NEW violation fails.
  #
  # Needs the shared packages built — the cruise resolves workspace imports
  # through the node_modules symlinks npm workspaces creates.
  dependency-cruiser:
    needs: packages-build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build shared packages
        uses: ./.github/actions/turbo-packages-build

      - name: Check architectural boundaries
        run: npm run check:dep-cruiser

  # ── Duplicate detection (jscpd) ─────────────────────────────────────────
  # ADVISORY on purpose — deliberately NOT in scripts/ci/lib/required-checks.mjs.
  #
  # jscpd has no clone-level baseline the way dependency-cruiser does, so the
  # only lever is a repo-wide duplication percentage. `.jscpd.json` pins it at
  # 4.5%, just above the 4.37% measured when this landed, which catches net-new
  # duplication but cannot distinguish "one bad copy-paste" from "the number
  # drifted". That is too coarse to block a merge on, and the ratchet only ever
  # moves down as the consolidation work lands.
  #
  # The job is simply allowed to FAIL when the threshold is breached. That is
  # what "advisory" means here: `duplicate-detection` is deliberately absent
  # from CI_CHECKS, so a red X reports loudly and blocks nothing.
  #
  # It must NOT be `continue-on-error`. That key rewrites the step's conclusion
  # to success, so the job goes green and a breached threshold is invisible —
  # an advisory gate nobody can see is not advisory, it is off.
  duplicate-detection:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check duplication against the repo-wide threshold
        run: npm run check:duplication

      # The console reporter prints the summary table only; the clone-by-clone
      # detail lives in the JSON report, which is the thing you actually need to
      # act on a threshold breach — so `always()`, or the artifact is missing in
      # precisely the run that needed it.
      - name: Upload duplication report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jscpd-report
          path: .jscpd/
          if-no-files-found: ignore
          retention-days: 14

  # ── Migration safety ────────────────────────────────────────────────────
  migration-safety:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check migration safety (PR)
        if: github.event_name == 'pull_request'
        run: npm run check:migration-safety -- --base "${{ github.event.pull_request.base.sha }}" --head "${{ github.sha }}"

      # Deliberately bare (no --base/--head) on push: the promotion-docs half
      # of this check is a PR-time review gate, not a merge-time one — by the
      # time a commit reaches `main` via merge, the PR run above (line 738)
      # already enforced it. Filename/version
      # validation (validateMigrationFiles) still runs unconditionally either
      # way, so a malformed migration filename is still caught on push (#980).
      - name: Check migration safety (push)
        if: github.event_name == 'push'
        run: npm run check:migration-safety

  # ── Migration lock safety (ADVISORY) ────────────────────────────────────
  # Squawk over the migrations this change touches, looking for the patterns
  # that take a heavy lock on a live table: a constraint added without
  # NOT VALID, a column added with a volatile default, a UNIQUE constraint that
  # builds its index under ACCESS EXCLUSIVE. Instant on an empty staging table;
  # a write outage on a production one.
  #
  # ADVISORY ON PURPOSE, and `continue-on-error` is the whole mechanism.
  # Squawk's default rules report 403 issues across 44 of this repo's 51
  # migrations. `.squawk.toml` excludes the five rules responsible for 390 of
  # them — two flag a fix that is IMPOSSIBLE under Supabase, which runs each
  # migration inside a transaction where CONCURRENTLY is illegal — leaving 13
  # real findings across 8 files. Those 13 are on already-deployed tables, so
  # blocking today would fail PRs for findings they did not introduce.
  # Promoting this to a gate needs a baseline of the existing 13, the same move
  # `.dependency-cruiser-known-violations.json` makes for the architecture gate.
  # `check-migration-lock-safety.mjs --strict` is the switch when that lands.
  #
  # NOT in scripts/ci/lib/required-checks.mjs, deliberately: an advisory
  # job that is also a required check is just a confusing gate.
  migration-lock-safety:
    runs-on: ubuntu-latest
    # Reports; never blocks. Removing this line makes it a merge gate — do not
    # do that without the baseline described above.
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # squawk-cli is a pinned devDependency (exact version, no caret) so a new
      # Squawk release cannot change what CI reports without a lockfile change.
      - name: Install dependencies
        run: npm ci

      - name: Lint changed migrations (PR)
        if: github.event_name == 'pull_request'
        run: npm run check:migration-lock-safety -- --base "${{ github.event.pull_request.base.sha }}" --head "${{ github.sha }}"

      - name: Lint changed migrations (push)
        if: github.event_name == 'push'
        run: npm run check:migration-lock-safety -- --base "${{ github.event.before }}" --head "${{ github.sha }}"

  # ── Chapter directory seed validation ───────────────────────────────────
  # Guards supabase/seed/chapter_directory.csv: canonical #RRGGBB colors,
  # archetypes that exist in the ArchetypeKey union, no duplicate natural keys.
  #
  # The colors are the reason this is blocking. The seed shipped with 50 of its
  # 100 values missing a leading `#` (#840), and nothing caught it because the
  # accent engine answers an unparseable hex with the house seed rather than an
  # error — so a bad value produces a plausible wrong brand color, not a failure.
  #
  # No `npm ci` step: the checker imports only node: builtins, so installing the
  # workspace would be pure latency. The loader's actual execution against a real
  # database is covered in `pglite-migrations` instead, the one job here with one.
  chapter-directory-seed:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Validate the chapter directory seed
        run: npm run check:chapter-directory-seed

  # ── Mobile app validation ───────────────────────────────────────────────
  mobile-validate:
    needs: packages-build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build shared packages
        uses: ./.github/actions/turbo-packages-build

      - name: Lint mobile
        run: npm run lint -w apps/mobile

      - name: Type check mobile
        run: npm run check-types -w apps/mobile

      # The mobile suite existed but ran nowhere, so its guards were free to
      # regress silently — #687 found one that had (reverting the quiet-hours
      # zone guard left every test green).
      - name: Mobile unit tests
        run: npm run test -w apps/mobile

  # ── Deploy-gate script unit tests ───────────────────────────────────────
  ci-scripts-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Run deploy-gate script tests
        # No npm ci needed — these tests have no dependencies beyond node:test.
        run: npm run test:ci-scripts

  # ── Dependency audit gate (issue #618) ──────────────────────────────────
  # Fast, standalone gate: no `npm ci` (the script audits the lockfile via
  # --package-lock-only). Fails on any HIGH/CRITICAL advisory not explicitly
  # allowlisted in scripts/npm-audit-allowlist.json — entries need a reason, a
  # tracking issue, and an expiry, so known debt is visible and time-boxed
  # while new advisories block immediately. See
  # docs/internal/security/SECURITY_FIXES.md § npm audit sweep + CI gate.
  dependency-audit:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: npm audit gate (high/critical block unless allowlisted)
        run: npm run check:npm-audit

  # ── Secret scanning (gitleaks; ADR-13 push-protection replacement) ──────
  # Fast, standalone gate: no `npm ci` (gitleaks is a single pinned binary). Scans
  # only the PR/push commit range against .gitleaks.toml. The pre-commit hook is the
  # local primary; this is the enforced server-side net. It was adopted to replace the
  # secret push-protection lost when the repo went private — the repo is public again, so
  # that premise has lapsed and the gitleaks-vs-GHAS choice is unmade rather than settled.
  # See docs/internal/ci-cd/SECRET_SCANNING.md.
  secret-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # scan-secrets.mjs fetches the pinned gitleaks binary on first run (no npm ci needed).
      - name: Scan PR commit range for secrets
        if: github.event_name == 'pull_request'
        run: node scripts/scan-secrets.mjs --base "${{ github.event.pull_request.base.sha }}" --head "${{ github.sha }}"

      - name: Scan pushed commit range for secrets
        if: github.event_name == 'push'
        run: node scripts/scan-secrets.mjs --base "${{ github.event.before }}" --head "${{ github.sha }}"

  # ── PGlite migration + RLS smoke validator (ADR-11 / ADR-12) ────────────
  # Applies every `supabase/migrations/*.sql` to a fresh Postgres-in-WASM
  # and asserts structural landmarks (unique indexes, generated columns) plus
  # an RLS smoke tier: every public table enables RLS (default-deny invariant)
  # and the chat hot-path RLS posture holds (default-deny channels/messages;
  # ownership-scoped reaction policies). Covers what static
  # `check:migration-safety` can't: the SQL actually runs and the schema lands.
  # It also runs the black-box *enforcement* tier: a non-owner `rls_probe` role
  # reads four tables for real — exact visibility sets on the two chat tables,
  # zero rows on `members` / `financial_invoices`, which must stay default-deny.
  # The `authenticated` role is created before migrations apply, so the ~18
  # migrations that guard policies behind `rolname = 'authenticated'` actually
  # run and their `to authenticated` clause binds the probe.
  # Out of reach: Realtime, Presence, GoTrue with real JWTs, and `to anon`
  # targeting (no `anon` role here, though `auth.role()` is stubbed to it).
  # Those live in the NestJS Jest tier (see ADR-11/ADR-12 + AGENT_INFRA.md).
  pglite-migrations:
    needs: changes
    if: ${{ github.event_name == 'push' || needs.changes.outputs.pglite == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply migrations to PGlite + assert schema landmarks
        run: npm run check:pglite-migrations

  # ── 375px responsive floor (REQUIRED) ───────────────────────────────────
  #
  # The only Playwright job in this workflow. It was split out of the advisory
  # `web-visual-regression` snapshot job in #1152 precisely so it could be
  # required, and that snapshot job has since been deleted outright — baselines
  # pinned to CI's Chromium build drifted with every bump, and an advisory gate
  # nobody could act on was costing more than it caught.
  #
  # This suite has none of those failure modes: it stores no baseline and
  # compares no pixels, reading one integer per route and comparing it to 375.
  # `spec/ui/web-dashboard/README.md` states the floor as a MUST; this job is
  # what makes that word mean something.
  #
  # Selection is by directory, not by tag. With the snapshot suite gone,
  # `test:floor` runs everything under `apps/web/tests/visual/`, so a NEW spec
  # added there joins this required job by default rather than falling through
  # into no job at all. Do not reintroduce a `--grep` filter without a second
  # job to catch what it excludes.
  #
  # No `continue-on-error`, per docs/internal/ci-cd/QUALITY_GATES.md: that key
  # rewrites the step's conclusion to success, which would leave this job green
  # on a breached floor — a required check that cannot fail is not a gate.
  #
  # The other way a gate goes hollow is running zero tests and exiting 0, which
  # would be worse: green, required, and asserting nothing. Playwright exits **1**
  # when a run collects no tests — verified by running it against the 1.62.1 that
  # `npm ci` resolves, not assumed; `apps/web/package.json` asks for `^1.62.1`, so
  # re-check on any upgrade that moves the lockfile. Note the narrowing that came
  # with directory selection: `--grep @floor` exited 1 whenever the FLOOR SUITE
  # went missing, while directory selection keys on the collected-test count, so a
  # SECOND spec added here would mask the floor spec's removal. See
  # apps/web/playwright.config.ts.
  #
  # Path-gated like the jobs around it. That does NOT conflict with being
  # required: the gate is a JOB-level `if:`, and GitHub reports a job skipped by
  # a conditional as Success. Both `needs:` parents are themselves required
  # checks, which is the invariant scripts/ci/lib/required-checks.mjs
  # spells out for `web-tests` — a required check whose parent is not required
  # is a hole, because a job skipped for a FAILED parent also reports skipped.
  web-responsive-floor:
    needs: [packages-build, changes]
    if: ${{ github.event_name == 'push' || needs.changes.outputs.web == 'true' }}
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build shared packages
        uses: ./.github/actions/turbo-packages-build

      - name: Resolve Playwright version
        id: pw
        run: echo "version=$(node -p "require('@playwright/test/package.json').version")" >> "$GITHUB_OUTPUT"

      # Keyed on the resolved Playwright version, so a version bump is a cold
      # key and this job downloads Chromium once before repopulating the cache.
      # This used to share the entry with `web-visual-regression`, which ran
      # concurrently; with that job deleted there is no second writer WITHIN a
      # run. There can still be one ACROSS runs: the concurrency guard above
      # stops main push runs cancelling each other, so two merges close together
      # can reach this step at once on the same version-keyed entry.
      # `actions/cache` settles that by letting the first reservation win and
      # warning on the second, so the "Unable to reserve cache" annotation is
      # harmless where it appears and the restore path is unaffected.
      - name: Cache Playwright browsers
        id: pw-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.pw.outputs.version }}

      - name: Install Playwright OS deps
        run: npx playwright install-deps chromium

      - name: Install Playwright browser
        if: steps.pw-cache.outputs.cache-hit != 'true'
        run: npx playwright install chromium

      - name: Assert every dashboard route holds the 375px floor
        run: npm run test:floor -w apps/web

      # No artifact upload, deliberately. The retired snapshot job needed one
      # because a pixel diff cannot be read from its logs; this suite's failure
      # message already names the offending route, the widest element inside
      # `<main>`, its width against the budget, and its classes.
