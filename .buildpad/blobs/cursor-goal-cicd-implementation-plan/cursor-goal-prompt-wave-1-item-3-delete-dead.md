Read `REFACTOR-PLAN.md`, section "Item 3 — dead @repo/ui package" ONLY (from PR #1095). Scope fence: only modify what that section lists — both `apps/web` and `apps/landing` dependency entries, the 4 configs, the CI step, and the 11 doc/spec/skill references, including the 2 skills that currently tell agents to `import { Button } from "@repo/ui/button"`. This item is docs-heavy, not just deletion — a prior draft of this plan skipped the docs and would have failed the required docs-sync gate. Don't repeat that.

Create `REFACTOR-PROGRESS.md` (or append to it if it exists) listing every file from the plan section as an unchecked item; check each off with a one-line note as you finish it.

Definition of done: `@repo/ui` package deleted, zero dependency references anywhere in the workspace, CI step removed, all 11 doc/spec/skill references updated to point at wherever those components actually live now, docs-sync gate passes, typecheck passes, `npm run build` succeeds for both apps/web and apps/landing. Paste a repo-wide `grep -r "@repo/ui"` (or equivalent) showing zero matches into the PR. Open a draft PR if anything remains — don't claim done without that zero-match proof.