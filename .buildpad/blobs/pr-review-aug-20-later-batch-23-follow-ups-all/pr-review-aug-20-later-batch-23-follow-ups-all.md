# PR review Aug 20 (later): Batch 2/3 follow-ups all merged + 4 dependabot bumps

**#1140 merged clean.** Both queued items done as scoped: `void-use-memo` enabled (last v7 rule, zero findings, done), and the 3 portable hooks moved into `@repo/hooks`. #1133/#1135 correctly left untouched. New debt found and filed: **#1139** — `*.tsbuildinfo` isn't gitignored, leaves a 641KB untracked artifact after `check-types`. Also flagged-not-fixed: an orphaned JSDoc on `use-org-config.ts` (cosmetic, deliberately left to keep the move-diff clean).

**#1136 merged clean — unprompted, and a bigger deal than it looks.** Claude Code independently ran down issue #1086 (GitHub MCP dedup regression) and found the real root cause: every MCP read path (`issue_read`, `list_issues`, `search_issues`) silently strips HTML comments and tags and entity-escapes text on the way out — verified with 11 live probe calls plus ground-truth `WebFetch` comparisons. This is a **different, deeper bug than the "serverStatus: error" outage** — the connection works, but reads quietly corrupt content, which is exactly why dedup markers (stored as HTML comments) kept behaving as if missing. Nothing was actually lost on GitHub's side. Fix: moved the `fp=` dedup marker format from an HTML comment to a visible text line (survives the read bug), added an explicit rule that no issue-body rewrite may ever be sourced from an MCP read (append a comment instead), and fixed 4 related bugs a `/diff-review` pass caught before merge (routines that would've re-broken this).

**Net effect of #1132/#1136/#1140 together:** the MCP/tracker-hygiene problem that drove the Cursor-retirement decision is now understood at the root-cause level, not just avoided by switching tools. Claude Code + this fix should be materially more reliable going forward than either Cursor was or Claude Code was before this PR.

**Remaining open items, unchanged:** #1133 (mobile tutorial replay — needs Paul's decision), #1135 (notification feed scope — needs Paul's decision), #1138 (human-only: required check + branch cleanup), #1139 (new, trivial — gitignore tsbuildinfo).