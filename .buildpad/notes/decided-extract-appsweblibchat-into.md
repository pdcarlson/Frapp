Decided: extract apps/web/lib/chat/ into packages/chat-core with 3 injected adapters (KeyValueStore, NetworkState, OutboxStore) rather than building mobile chat on the orphaned use-chat.ts hooks. ~1223 lines move untouched, ~1387 need small seam injection, only offline-queue.ts (~185 lines, Dexie/IndexedDB) needs a real mobile rewrite (AsyncStorage/SQLite equivalent). Web refactor (steps 1-4) is a pure refactor with existing test coverage, worth doing regardless of mobile timeline. Deleting orphaned use-chat.ts hooks is independent cleanup, can land immediately. Risk: realtime reconnect logic is browser online/offline-event driven, needs AppState-driven transitions for mobile backgrounding \u2014 new behavior, not just a port.