Workflow change (Aug 19): Paul committed the full Buildpad canvas into the repo under `.buildpad/` (blobs/, documents/, notes/), synced periodically via git. Agents (Claude Code and Cursor) can now read canvas research/audits directly from the filesystem instead of needing documents pasted or attached in chat. All prompts should point at `.buildpad/` rather than saying "I've attached X" going forward. Treat `.buildpad/` as planning/research background, not a spec source of truth — it can contain stale or superseded ideas (that's the nature of a running brainstorm), and it should be excluded from lint/duplication/boundary tooling since it's not source code.