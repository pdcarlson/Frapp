import type { ReactNode } from "react";

export const metadata = {
  title: "Members | Frapp",
  description: "Chapter roster, invites, and member directory.",
};

export default function MembersLayout({ children }: { children: ReactNode }) {
  return children;
}
