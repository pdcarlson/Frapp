import type { ReactNode } from "react";

export const metadata = {
  title: "Points | Frapp",
  description: "Points ledger, leaderboard, and adjustments for the active chapter.",
};

export default function PointsLayout({ children }: { children: ReactNode }) {
  return children;
}
