import { DocumentsPage } from "@/components/documents/documents-page";

export const metadata = {
  title: "Chapter documents — Signet",
};

export default function Documents() {
  return <DocumentsPage />;
}
